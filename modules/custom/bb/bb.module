<?php

/**
 * Implements hook_views_api().
 */
function bb_views_api()
{
  return array(
    'api' => 3,
    //'path' => drupal_get_path('module', 'expose') . '/includes/views',
    'path' => '.',
                            );
}
function bb_theme($existing, $type, $theme, $path) {

  return array(
    // theme pour src/SessionsTableForm.php
    // le template est appelé ci-dessous
    // voir themes/bbtheme/session-form.html.twig 
    'session' => array(
      'template' => 'session-form',
      'render element' => 'form',

    ),
  );

}

